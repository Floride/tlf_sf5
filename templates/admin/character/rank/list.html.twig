{# templates\admin\character\rank\list.html.twig #}
{% extends 'admin/layout.html.twig' %}

{% block body_content %}
    
    {% include "admin/elements/_header.html.twig" with {
            'h_title':'Personnage : Grades',
            'h_path_retour':'admin_list'
        }
    %}

    {% include "admin/elements/_alert.html.twig" %}
    
    {% set sh_description = "" %}
    
    {% include "admin/elements/_sub_header.html.twig" with {
            'sh_description':sh_description,
            'sh_lien':'admin_character_rank_new',
            'sh_btn_txt':'Nouveau grade'
        }
    %}

    {% include "admin/elements/_display.html.twig" with {'route': 'admin_character_rank_list'} %}

    {% if display == 'card' %}
    
        {% include "admin/character/rank/_card.html.twig" %}

    {% else %}
    
        {% include "admin/character/rank/_list.html.twig" %}

    {% endif %}

    {# display navigation #}
    <div class="navigation text-center">
        {{ knp_pagination_render(ranks) }}
    </div>

{% endblock %}