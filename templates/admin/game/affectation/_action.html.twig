{# templates\admin\game\affectation\_action.html.twig #}
<a href="{{ path('admin_game_affectation_edit', {id: affectation.data.id}) }}" class="btn btn-sm btn-primary">
    <i class="far fa-edit" title="Editer"></i>
</a>

<form action="{{ path('admin_game_affectation_delete', {id: affectation.data.id}) }}" 
    method="POST" id="delete_form_{{ affectation.data.id }}" class="form_delete"
    onSubmit="return confirm('Êtes-vous sur de vouloir supprimer cette affectation ?')"
/>
    <button type="submit" class="btn btn-sm btn-danger" id="submit_btn_{{ affectation.data.id }}">
        <i class="far fa-trash-alt" title="Supprimer"></i>
    </button>
    <input type="hidden" id="method_{{ affectation.data.id }}" name="_method" value="DELETE" />
    <input type="hidden" id="token_{{ affectation.data.id }}" name="_token" value="{{ csrf_token('admin_game_affectation_delete_' ~ affectation.data.id) }}" />
</form>